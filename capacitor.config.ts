import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'LolDataProject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
